# 多棋手与双向对局设计
用户已同意：添加多名棋手、选择已有棋手对局后更新双方、保留临时对手估分方式及个人成长视角。本稿细化已批准范围，实施不重复请求确认。

## 产品与流程
棋手入口显示姓名、当前 ELO、盘数，可新增棋手（姓名、初始 ELO，或段位基准填入），按姓名搜索并切换当前棋手。各棋手分别查看成长、分析、历史和设置。采用已有黑金视觉。
记一盘保留临时对手模式，新增已有棋手模式；排除自己，显示双方赛前分及胜负预计分差，点击当前棋手胜/负后一次保存双方结果。
已有棋手使用实时赛前分，K=20，无取整；临时对手按已批准段位/战绩规则，无样本平滑。
新对局允许更正胜负、删除、撤销；操作通过全局顺序重放所有受影响棋手，不能留下半条记录。已有棋手模式不允许修改对手身份（需要删后重新记）。
仅新增和重命名棋手，不提供有历史棋手删除；同名使用稳定 ID，列表显示短 ID 区分。

## 数据与迁移
比较：在每人档案复制对局会产生双份同步问题；保存统一对局并按棋手投影更稳妥，采用后者。
AppState 保留 profile 为当前选中棋手、matches 为全局唯一记录，增加 otherProfiles。所有棋手通过 allProfiles 访问。
Match 增加 playerId（默认 local）、opponentPlayerId（可空）、opponentEloAfter（可空）。已有对手使用 elo-pair-v1，临时对手新记录使用 elo-v2；历史 elo-v1 和旧记录 legacy-fixed-v1 均保留并按各自规则重放。
按 order 从所有 initialElo 起点重放；已有对手分差严格互为相反数。投影第二方时交换赛前赛后分，反转胜负和分差；视图副本绝不写回数据库。
Room v1→v2 保留原 profile/matches；新增三列及 meta.activePlayerId，旧局 playerId=local。事务更新全部档案及对局。
备份 v2 包含所有棋手和统一历史；严格接受 v1 结构并自动升级，不伪造旧文件中只有姓名的对手档案；拒绝无效外键、自我对局、重复 ID、分值不一致、更高格式版本。
恢复预览显示总棋手数、总对局数和选中档案；恢复前仍保存全部数据快照。

## 验证与交付
测试等分胜负2010/1990、跨三人对局后更正/删除重算、第二方视角、临时记录隔离、重复请求、自己对局拒绝、切换持久化、v1导入及v2往返。
数据库迁移使用旧v1真实表结构夹具测试；若无设备，明确报告设备测试仅编译。
版本0.3.0/versionCode3，同applicationId和签名；构建、单元测试、lint及设备测试编译后交付APK并更新本地仓库dist。无远程不声称已推送。
